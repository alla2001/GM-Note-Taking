{
  "name": "gm-note-taking",
  "version": "1.0.0",
  "description": "Backend service for a Game Master note-taking app (Express + MongoDB).",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/dev-server.js",
    "start:mongo": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node scripts/init-db.js",
    "test": "jest --runInBand --detectOpenHandles"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb-memory-server": "^9.2.0",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
